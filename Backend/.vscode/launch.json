{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Backend",
      "type": "coreclr",
      "request": "attach",
      "preLaunchTask": "start backend functions",
      "processId": "${command:azureFunctions.pickProcess}"
    }
  ]
}
