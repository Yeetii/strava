meta {
  name: Trigger QueueAllScrapeJobs (urgent, all)
  type: http
  seq: 9
}

post {
  url: {{backendUrl}}/api/manage/queue-all-scrape-jobs
  body: none
  auth: none
}

params:query {
  ~code: {{functionKey}}
}
