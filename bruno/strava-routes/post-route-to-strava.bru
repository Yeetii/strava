meta {
  name: Upload GPX to Strava
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/strava/routes?name=Morning+Run&description=Uploaded+from+GPX
  body: text
  auth: none
}

headers {
  Cookie: session={{sessionId}}
  Content-Type: application/octet-stream
}

body:text {
  <!-- paste raw GPX content here -->
}
